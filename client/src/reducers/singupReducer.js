import { singupState } from '../actions';
const singupReducer = (state = singupState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default singupReducer;
